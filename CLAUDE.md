# CLAUDE.md — PMPO 프로젝트 컨텍스트

> 이 문서는 Claude Code가 세션을 시작할 때 자동으로 참고하는 프로젝트 컨텍스트입니다.
> PRD → IA → 화면설계 → UI 디자인까지의 모든 기획 결정과 근거가 담겨 있습니다.
> 개발 시 이 문서를 기준으로 작업하고, 새로운 기능/정책 결정이 생기면 이 문서도 함께 업데이트해주세요.

---

## 0. 프로젝트 한눈에 보기

- **프로젝트명**: PMPO — PM/PO를 위한 질문·정보 커뮤니티 플랫폼
- **목적**: (1) 실제 창업, (2) 대기업 PM 포지션 지원을 위한 포트폴리오
- **운영자**: 개발 지식 없음. Claude Code를 통해 개발 진행 중
- **기술 스택**: Next.js (TypeScript) + Supabase (PostgreSQL, Auth) + Vercel 배포
- **현재 상태**: 기획 전 과정 완료 + 개발도 배포 전 필수 작업 전부 완료(회원가입/로그인/Circle(구 커뮤니티)/Insights(구 정보게시판)/신고·차단/컬러 시스템 v1.1/Circle·Insights 명칭 변경+모바일 대응/Medium 스타일 레이아웃·카드·전역 푸터/회원가입 강화·닉네임 제한·관리자 페이지 분리·Circle 글쓰기 개편까지 전부 라이브 반영됨). 남은 건 법률 검토 등 운영자 액션뿐 — 5장 "세션 핸드오프" 참고

### 검증된 문제의식 (배경)

PMPO 관련 정보를 요약 정리해주는 카카오톡 채널을 6개월간 운영(초기 100명, 이탈률 10% 내외 유지)하며 정보 소비 수요를 검증했고, 그 과정에서 "질문은 올라오지만 답변이 달리지 않는 문제"를 실제로 관찰함. 이 서비스는 그 문제("답변이 자연스럽게 생성되지 않는 구조")를 해결하는 것을 핵심 목표로 함.

---

## 1. PRD (제품 요구사항)

### 배경 (Background)

트렌드, 데이터, 업무 방식, 생산성 등 PM 관련 정보를 여기저기 찾아다녀야 하고, 진행 중인 프로젝트/프로덕트 정보를 공유·공유받을 곳이 마땅치 않음. 슬랙, X, 카카오톡 채널, 브런치, 텔레그램, 밴드, 스레드 등이 있지만 관련 정보를 찾기 어렵고 폐쇄적인 채널이 많으며, PM/PO 전문 플랫폼이 아니라 신뢰성이 떨어짐.

이를 검증하기 위해 PMPO 관련 정보를 요약 정리해주는 카카오톡 채널을 만들어 초기 유입자 100명을 모집해 운영했고 6개월 이상 이탈률 10% 내외로 유지됨. 해당 채팅방에서 현직 주니어 PM이나 PM 취업희망자가 질문을 올렸으나 답변이 없었던 사례를 관찰함.

### 문제 정의 (Problem)

PM/PO 직종자, 취업 희망자 등이 프로덕트 관련 정보나 트렌드, AI 관련 정보를 얻을 수 있고, 커뮤니티로 서로 소통하며 의견을 나누거나 해답을 얻을 수 있는 전문 PMPO 서비스가 없음. 또 자신이 필요한 프로덕트 관련 질문에 대한 답변을 받을 수 없음.

> **설계 결정**: 여기서 "질문"은 격식 있는 Q&A(선생님-제자 구조)가 아니라, 가볍게 묻고 답하는 대화 전반을 포함하는 개념. 이에 따라 커뮤니티는 별도 "Q&A 게시판"이 아니라 자유로운 대화형 단일 피드로 설계 (IA와 일치).

### 타겟 유저 (Target User)

1. 주니어 PM(1-5년차) — 본인 프로덕트에 맞는 정보 요구 수요자
2. PM 취업 희망자 — 기업 및 PM 트렌드 정보 수요자
3. 시니어 PM(6년차) — PM 트렌드 및 AI 관련 정보 수요자

### 목표 (Goal)

- 유저가 얻는 것: 다양한 정보 / 커뮤니티를 통한 소통
- 관리자가 얻는 것: 트래픽 / 이후 유료 자료

### 왜 이 솔루션인가 (Alternatives Considered)

1. PMPO 전문 플랫폼이 없음. '데이터리안'처럼 한 분야를 전문으로 하는 서비스는 정보·소통 필터링이 있어 더 명확하게 인식되고 이용하기 편함.
2. 카카오톡 그룹채팅은 초대 기반이라 폐쇄적임. 누구나 쉽게 접근 가능한 플랫폼이 필요함.

### 핵심 유저 시나리오 (Core User Flow)

1. **주니어 PM** — 본인 프로덕트 문제 해결을 위해 게시글을 찾아보고, 못 찾으면 커뮤니티에서 검색
2. **PM 취업 희망자** — 트렌드 파악을 위해 게시글 확인, 커뮤니티에 취업 관련 질문
3. **시니어 PM** — 딥한 정보/AI/트렌드 확인 후, 궁금해서 커뮤니티 답변을 확인하러 방문하고 가능하면 답변

### 핵심 기능 목록 (Must-have)

1. 커뮤니티에 궁금한 게시글을 올리고 댓글로 소통하는 커뮤니티
   - 게시글 작성 시 **"질문있어요" 태그 선택 가능 (선택사항)**
2. PMPO 관련 글을 올리는 기능 (관리자 관점)
   - 작성은 관리자만 가능하지만, **좋아요/댓글은 커뮤니티와 동일 규칙으로 유저에게 허용** (1depth, 대댓글/공유 없음)
3. 우수 답변 및 팔로우 수에 대한 보상/인정 장치
   - 답변에 좋아요 기능
   - 팔로우 수에 따른 뱃지 삽입 기능 (기준: **팔로우 30명 이상**)
4. 커뮤니티 게시글 목록 정렬/필터에 **"'질문' 태그된 글 중 댓글 없는 것만 보기"** 옵션 추가
5. **작성자 프로필/닉네임 클릭 시 팔로우 가능**
6. **게시글에 이미지 첨부 기능(선택)** — Supabase Storage 기반. 이미지 있는 글은 썸네일 우선 노출, 이미지만 있는 글도 제목은 필수

### 제외 범위 (Out of Scope)

1. 프로필에서 게시글을 보거나 모아볼 수 있는 기능
2. 시니어나 인지도 있는 PMPO가 작성하는 글의 유료 구독 기능
3. 기업과 연차가 확인될 경우 인증 PM 문구 삽입
4. 댓글에 대한 대댓글(스레드) 기능
5. 게시글/댓글 공유 기능

### 성공 지표 (Success Metric)

1. '질문' 태그된 게시글 중 답변(댓글)이 1개 이상이라도 달린 비율 **80% 이상**
2. 첫 질문 후 답변(댓글)이 달리는 시간 **2시간 이내**
3. (보조지표) 정보 게시글 업로드 후 1주일 안에 3천뷰 이상

### 제약 조건 (Constraints)

1. 예산 부족
2. 개발 지식 없음
3. PM 실무 능력(중) — 실무적 디테일 부족
4. 출시 전 필요 사항: 이용약관, 개인정보처리방침, 회원가입 동의 UI 등

### 오픈 질문 (Open Questions)

1. ~~게시글 형태 고민~~ → **해소**: Medium 스타일 리스트형 게시판 확정
2. 수익화 방향 — 유료 뉴스레터 고려 중이나 방향성 미확정
3. "질문" 태그 없는 순수 정보공유 글이 댓글 없을 시 성공지표(80%)를 왜곡할 가능성 — 개발 단계에서 필요시 보완 검토
4. ~~비밀번호 재설정 시 미가입 이메일 처리~~ → **해소**: `/reset` 구현 완료(2026-08-01), Supabase `resetPasswordForEmail`이 기본적으로 이메일 존재 여부와 무관하게 항상 성공 응답을 반환해 보안 권장 방식 그대로 적용됨
5. ~~정보게시판 카테고리 탭 배치/디자인 세부사항~~ → **해소**: 데스크톱은 사이드바 2depth(IA v0.3), 모바일 웹은 Insights 목록 페이지 상단 가로 스크롤 탭으로 확정(2026-08-02)

### 리스크 및 대응 (Risks & Mitigations)

1. 유저 신고 기능
2. 관리자의 유저 게시글/댓글 작성 블락 기능
3. 키워드 확인 후 차단 기능
4. 무성의하거나 지속적인 홍보/자극적 언어 행동 → 추후 고민

### 백로그 (이후 버전에서 고려)

1. 댓글/게시글 공유 기능
2. 대댓글(스레드) 기능
3. 커뮤니티 카테고리 분류 (질문 수 쌓인 뒤 데이터 기반 재검토)
4. 라이브러리(북마크/모아보기) 메뉴
5. 영상 직접 업로드
6. "답변완료" 상태 배지 신규 도입 — 컬러 시스템 v1.1에 `--answered`(#3F7A5C) 토큰은 정의해뒀지만, 실제로 댓글이 달린 질문에 "답변완료" 표시를 해주는 배지 UI 자체가 아직 없음(현재는 "답변 없는 질문만 보기" 필터만 존재). 필요 시 향후 추가 검토
7. 로고에 코랄(`--accent`) 포인트 요소 추가 — 컬러 시스템 v1.1 문서상 로고는 "기존 결정(B안) 유지: 'The PMPO' 전체 ink, 코랄은 점/사각형/밑줄 포인트로만"이라고 되어 있으나, 실제 코드(`app/components/Sidebar.tsx`)에는 순수 텍스트만 있고 포인트 요소가 구현된 적이 없었음(관련 SVG/로고 파일도 없음). 2026-08-02 감사에서 발견, 현재는 그대로(포인트 없음) 유지하기로 결정 — 필요 시 향후 구체적 형태(마침표/점/밑줄 등) 정해서 추가
6. 사이드바 "인기 게시글" 노출 (데스크톱 디자인 단계에서 제안된 신규 아이디어, 정식 반영 여부 미정)

---

## 2. IA (정보구조)

> **명칭 변경 반영 (IA v0.3, 2026-08-02)**: "커뮤니티" → **Circle**, "정보게시판" → **Insights**, 카테고리 "프로덕트/PM·PO 트렌드" → **Product/Trends** (AI는 그대로). 상세 근거는 `docs/PMPO_IA_v0.3.md` 참고.
> **구조 갱신 (2026-08-03~08-05)**: 아래 사이트맵/로그인 필요 여부 표는 그 사이 실제로 바뀐 코드 기준으로 다시 갱신함 — v0.3 문서가 규정했던 "Insights 사이드바 2depth 상시 노출" 구조는 2026-08-04(`b61ea60`)에 **플랫 내비게이션으로 되돌아감**(아래 3장 참고), `/home`·`/notice` 라우트가 새로 생김, Circle 게시글 상세가 로그인 필요로 정책이 바뀜.

### 사이트맵

```
Home (`/home`, 2026-08-03 신규) — every.to 스타일 에디토리얼 랜딩. 최신 Insights, 인기글, Circle 인기글 노출
├── Insights (관리자 발행 아티클)
│   ├── Product / Trends / AI — 좌측 내비에 Circle과 동일한 1depth 항목으로 노출(2depth 아님, 아래 3장 참고)
│   │   ├── 아티클 목록 (카테고리별 고유 URL: /insights/product, /insights/trend, /insights/ai)
│   │   └── 아티클 상세 (좋아요/댓글 가능, 비로그인도 열람 가능)
├── Circle (질문/답변, 카테고리 없는 단일 피드)
│   ├── 게시글 목록 (정렬/필터: 최신순, 답변 없는 질문만 보기), 본문 미리보기+썸네일 노출(2026-08-03), 비로그인도 열람 가능
│   │   └── 게시글 상세 (댓글, 좋아요, 본문 전체+이미지 노출) — **2026-08-04부터 로그인 필요**(아래 표 참고)
│   └── 질문 글쓰기 — 전용 페이지 `/circle/new` (2026-08-03, 기존 목록 인라인 작성폼에서 분리)
├── Notice (`/notice`, 2026-08-03 신규) — 공지사항 자리만 마련된 placeholder 페이지. 현재 좌측 내비/모바일 탭바 어디에도 링크가 연결되어 있지 않은 미완성 상태(직접 URL로만 접근 가능)
├── 마이페이지 (로그인 시)
│   ├── 프로필 (뱃지, 팔로워/팔로잉 수 → 클릭 시 목록, 아바타 업로드 2026-08-03 추가)
│   └── 내 정보 수정 (회원 탈퇴 포함)
└── 로그인 / 회원가입
    ├── 로그인 / 회원가입(약관 동의 포함) / 비밀번호 재설정 / 로그아웃
```

### 로그인 필요 여부

| 페이지/액션 | 비로그인 | 로그인 필요 |
|---|---|---|
| Insights 목록/상세 보기 | ✅ | - |
| Circle 목록 보기 | ✅ | - |
| **Circle 게시글 상세 보기** (2026-08-04 정책 변경) | ❌ | ✅ |
| 질문 글쓰기 / 댓글 작성 / 좋아요 / 팔로우 | ❌ | ✅ |
| 마이페이지 | ❌ | ✅ |

- **Circle 상세를 로그인 필요로 바꾼 이유(2026-08-04, `57653b5`)**: Circle 목록·Insights는 계속 완전 공개 유지, Circle 게시글 상세만 비로그인 접근 시 로그인 유도 모달을 띄우도록 변경. 목록 카드 클릭 단계(`CirclePostCard`/`HomeSidebar`/`EditorialHome`)와 상세 라우트 자체(직접 URL 접근 시에도 auth 확인 전에는 데이터를 아예 불러오지 않음) 양쪽에서 동시에 강제해 URL 직접 공유로 우회할 수 없게 함. **이 표는 이전 버전(IA v0.3, "Insights·Circle 목록/상세 모두 비로그인 열람 가능")과 다름 — 실제 코드가 기준**

### 설계 결정 요약

- Insights(관리자 발행) vs Circle(유저 질문) 분리, 단 소비 방식(좋아요/댓글)은 동일 규칙 공유
- Circle은 카테고리 없이 단일 피드 (초기 진입장벽 최소화, 추후 데이터 쌓이면 카테고리화 재검토)
- Insights는 처음부터 Product/Trends/AI 3분류 유지 (콘텐츠 소비 목적이 뚜렷해 카테고리가 유효하다고 판단), 카테고리별 고유 URL을 가져 직접 링크 공유·뒤로가기가 정상 동작. 카테고리 선택 UI 위치는 사이드바 2depth(v0.3, 2026-08-02) → 플랫 내비 항목(2026-08-04, `b61ea60`)으로 다시 바뀜 — 아래 3장 참고
- 읽기는 비로그인 허용(트래픽 확보) 원칙이었으나, Circle 상세만 예외로 로그인 필요로 전환(위 표 참고) — 나머지 읽기 페이지는 원칙 그대로 유지
- 라이브러리(북마크) 메뉴 제외 — Out of Scope와 목적 중복
- "Circle" 메타포는 "질문있어요" 태그·Insights 쪽에는 확장 적용하지 않음(상징 요소가 여러 개 겹치면 브랜드가 흐려진다는 판단)

---

## 3. 화면설계 (와이어프레임) — 총 10개 화면

> **명칭 변경 및 카테고리 탭 제거 반영 (화면설계 v0.8, 2026-08-02)**: 아래 화면 1~4는 v0.8 기준(Circle/Insights 명칭). ⚠️ v0.8이 규정했던 "Insights 카테고리 탭 UI 제거 → 사이드바 2depth로 이동"은 2026-08-04(`b61ea60`)에 다시 뒤집혀, 현재는 좌측 내비의 플랫 항목(Product/Trends/AI)으로 카테고리를 선택함 — 2depth 사이드바 자체가 없어짐(2장/이 절 하단 "데스크톱 레이아웃 규칙" 참고). 상세는 `docs/PMPO_화면설계_v0.8.md` 참고(단, 2depth 관련 서술은 최신 코드와 다름).

1. Circle 게시글 목록 — 무한스크롤, 인기 댓글(좋아요 상위 2개) 미리보기, 카드 타입별(텍스트/이미지/텍스트+이미지) 레이아웃, 제목("Circle") 아래 소개 카피("질문하고 답하며 함께 성장하는 자리예요") 노출(v0.8 신규)
2. Circle 게시글 상세 — 댓글 1depth, 좋아요, 팔로우 버튼, 대댓글/공유 없음, **2026-08-04부터 비로그인 접근 시 로그인 유도 모달로 대체됨**(2장 "로그인 필요 여부" 참고)
3. Insights 목록 — 카테고리(Product/Trends/AI)는 좌측 내비의 플랫 항목에서 선택(당초 v0.8의 "사이드바 2depth"에서 2026-08-04에 재변경), 좋아요/댓글 수 노출
4. Insights 상세 — 좋아요/댓글 Circle과 동일 규칙, 조회수는 로그인 유저=user_id / 비로그인=브라우저 익명 쿠키 기준 24시간 중복 제외
5. 로그인
6. 회원가입 — 약관 동의 UI (전체동의 + 필수 3개: 만14세 이상/이용약관/개인정보처리방침 + 선택 1개: 마케팅수신), `user_agreements` 테이블에 동의 시각 기록
7. 비밀번호 재설정
8. 마이페이지 프로필 — 팔로워/팔로잉 수(클릭 시 화면10 이동), 뱃지(팔로우 30명 이상)
9. 내 정보 수정 — 닉네임/비밀번호 변경, 회원 탈퇴(화면 우측 하단에 눈에 안 띄게 배치)
10. 팔로워/팔로잉 목록

**회원 탈퇴 정책**: 닉네임 유지, 프로필 사진은 기본 이미지로 리셋, 작성한 게시글/댓글은 삭제하지 않고 유지.

화면 1~4를 제외한 나머지(로그인/회원가입/비밀번호 재설정/마이페이지/내 정보 수정/팔로워·팔로잉 목록)의 상세 기능 명세는 명칭 변경의 영향을 받지 않아 `PMPO_화면설계_v0.7.md`를 그대로 참고. 각 화면의 그레이스케일 와이어프레임 이미지는 `wireframe_images/` 폴더 참고(색상은 v1.0 팔레트 기준 톤 참고용).

### 데스크톱 레이아웃 규칙

> **내비게이션 컴포넌트 교체 (2026-08-05, `bc1899b`)**: 기존 `Sidebar.tsx`는 완전히 폐기되고 `app/components/home/WorkspaceFrame.tsx` 하나로 통합됨(`AppShell`이 더 이상 라우트별로 다른 내비 컴포넌트를 분기하지 않음 — `app/components/AppShell.tsx`는 `/login`~`/reset/confirm`(독립 카드), `/home`(자체 헤더), `/`·`/post/*`(자체적으로 `WorkspaceFrame`을 내부에서 사용), 나머지 전부(`WorkspaceFrame`로 감쌈) 4갈래로만 분기).

- 좌측 내비 메뉴는 **`WorkspaceFrame`의 플랫 리스트**(`WORKSPACE_NAV_ITEMS`): Home / Circle / Product / Trends / AI — **2depth 없음**. v0.3에서 결정했던 "Insights 1depth + Product/Trends/AI 2depth 상시 노출" 구조는 2026-08-04(`b61ea60`, "revert desktop left nav back to the flat ... list")에 다시 플랫 구조로 되돌아감(Insights라는 상위 항목 자체가 없어짐, Product/Trends/AI가 Circle과 동급 항목으로 나열됨)
- 활성 상태 표시도 함께 바뀜 — 자세한 스타일은 아래 "4. UI 디자인 시스템"의 "내비게이션 활성 상태" 참고(색상 기반 규칙 → `bg-black/[0.04]`+굵게+왼쪽 라인 규칙으로 전환)
- 로그인 시 내비 하단에 "팔로잉"(최근 5명, `WorkspaceDataContext`에서 fetch) 미리보기 목록 노출, 5명 초과 시 "더보기" 링크로 팔로잉 목록 페이지 이동(이름 클릭은 비활성 — 2026-08-06 수정, 아래 5장 참고)
- 콘텐츠형 화면: 좌측 내비 + 중앙 본문(목록은 넓게, 상세는 읽기 폭 ~600px로 좁게) + 우측 보조 사이드바(글쓰기 유도)
- 로그인 전 인증 화면: 내비 없이 독립된 중앙 카드
- 로그인 후 폼 화면(마이페이지 등): 좌측 내비 + 중앙 카드
- 기준 해상도: 콘텐츠 영역 1280~1440px

### 모바일 웹 레이아웃 규칙 (2026-08-02 신규, 네이티브 앱 아님 · 반응형 CSS · 2026-08-04~05 갱신)

- 분기 기준: Tailwind `lg`(1024px) — 프로젝트에 기존에 있던 유일한 반응형 분기(`app/page.tsx`의 우측 프로모 사이드바 `hidden lg:flex`) 기준을 그대로 따름
- 하단 고정(`position: fixed`) 탭바(`app/components/MobileTabBar.tsx`), 순서는 **Insights / Circle / 마이페이지**(2026-08-04, `5dc31e8`로 이 순서로 재정렬). iOS 하단 홈 인디케이터와 안 겹치게 `env(safe-area-inset-bottom)` 패딩 적용
- **Insights 탭은 개별 라우트가 아니라 탭 버튼**으로, 누르면 Product/Trends/AI 3개 항목이 담긴 드롭업(dropup) 패널이 뜸(`getBoundingClientRect()`로 "Insights" 라벨 위치에 맞춰 좌측 정렬). 기존에 있던 "Insights 카테고리 목록 페이지 상단 가로 스크롤 탭"(`ArticleListPage.tsx`)은 이 드롭업 도입 이후에도 그대로 유지되어 두 UI가 공존함(목록 페이지엔 상단 탭, 하단 탭바엔 드롭업)
- 활성 상태는 `text-accent`(활성) vs `text-ink-soft`(비활성)로 데스크톱 `WorkspaceFrame`과는 다른 색상 기반 규칙을 그대로 유지(모바일 탭바는 이번 개편의 영향을 받지 않음)
- 하단 탭바와 겹치지 않도록 본문 영역에 모바일에서만 하단 패딩 추가 — "+글쓰기"/"+새 글 작성" 버튼 위치 자체는 변경 없음
- 하단 탭바의 마이페이지 아이콘은 로그인 전/후 아이콘을 다르게 하지 않음, 클릭 시에만 로그인 여부로 목적지 분기(`/login` vs `/profile/[id]`)
- 아이콘은 `app/components/icons.tsx`로 공용화 — `stroke="currentColor"`로 만들어 부모 요소의 텍스트 색을 그대로 상속받는 방식

---

## 4. UI 디자인 시스템

> **컬러 시스템 v1.1(색상 절충안)로 전환 완료 (2026-08-02)**. 아래는 v1.1 기준. v1.0(다색 팔레트) 상세 내역은 `docs/PMPO_컬러시스템_v1.1_확정본.md`에 대비표로 남아있음.

- **브랜드명 표기**: "The PMPO"
- **컬러 원칙**: 화면의 90% 이상은 무채색, 색은 버튼/링크(hover)/활성 탭/로고에만 등장. 상태 표시(질문 등)는 배경을 채운 칩이 아니라 아이콘/텍스트 색만으로 최소 표시
- **컬러 토큰**: Paper `#FAF8F6` / Surface `#FFFFFF` / Ink `#262322` / Ink Soft `#6B6460` / Border `#E5E1DD` / Accent(유일한 포인트 컬러 — 버튼·링크 hover·활성 탭·로고) `#C8464D` / Accent Hover `#A83840` / Question(질문 태그, 배경 없음) `#B8791C` / Error(폼 검증, 배경 없음) `#B8382E`
  - `--answered`(#3F7A5C, 답변완료 상태용)는 토큰만 정의, 실제 배지 UI는 미구현 — 백로그 참고
- **타이포그래피**: 한글/영문 — Noto Sans CJK KR (Bold 헤더 / Medium·Regular 본문), 메타정보 — DejaVu Sans Mono
- **아이콘**: 이모지 대신 선(stroke) 기반 SVG 아이콘 사용
- **카드 스타일 (2026-08-03 변경)**: 목록 카드(Circle/Insights)는 더 이상 박스+그림자 형태가 아님 — 배경/테두리 없이 얇은 `border-b` 구분선만 있는 리스트 형태로 전환. Hover 시에만 왼쪽 2px `--accent` 라인 등장(투명 보더→accent 색 전환 방식이라 레이아웃 시프트 없음). "질문있어요" 카드는 평소엔 question색 왼쪽 라인이 떠 있다가, hover하면 그 순간만 accent색으로 바뀜(콘텐츠 신호 vs 인터랙션 신호를 하나의 라인으로 이원화). 게시글/아티클 **상세** 페이지는 기존처럼 박스+그림자 유지(목록만 변경)
- **컴포넌트 규칙**: Insights는 Medium 스타일(텍스트 좌/썸네일 우, 날짜·통계 줄 분리) — Circle 목록도 동일 패턴(본문 미리보기 2줄+썸네일) 적용(2026-08-03). 계정 탈퇴는 최하단 우측에 작게 배치
- **레이아웃 (2026-08-03 변경)**: 전체를 1280px 컨테이너로 가운데 정렬하던 방식 폐기 → 사이드바는 뷰포트 왼쪽 끝에 고정, 우측에 1px `--border` 세로 구분선, 본문은 사이드바 오른쪽 "남은 공간" 안에서 `mx-auto`로 가운데 정렬(고정 gap 숫자 없음, 화면이 넓을수록 자연히 여백도 넓어짐 — Medium 참고). 본문 최대 폭: Circle/Insights 목록 `1360px`, 상세 `1280px`(2026-08-03, 기존 680/640px에서 사용자 요청으로 2배 확대). 본문에 `min-height: calc(100vh - 2rem)` 적용해 콘텐츠가 적어도 최소 한 번은 스크롤해야 푸터가 보이게 함
  - ⚠️ 구현 함정: 처음엔 `<main>` 자체를 `flex justify-center`로 만들어서 가운데 정렬했는데, 이러면 `<main>`의 자식(Circle의 "본문+우측 프로모박스" 묶음 같은 것)이 flex 자식으로서 "가용 공간껏 늘어나기"가 아니라 "필요한 만큼만(shrink-to-fit)" 크기가 잡혀서, 내부의 `flex-1` 자식이 커지지 않는 버그가 있었음. `<main>`은 평범한 block으로 두고, 각 페이지 자신의 루트 요소에 `max-w-[...] mx-auto`를 직접 붙이는 방식(고전적인 CSS 중앙정렬)으로 수정
- **전역 푸터 (2026-08-03 신규)**: `--ink` 배경의 다크 푸터, 라벨 없이 3줄(브랜드명+모토 / 이메일 / 이름·사업자번호·저작권) + 우측 상단에 SNS 아이콘 빈 자리 2개(링크 미정). 로그인/회원가입 포함 전 페이지 공통 노출. 모바일 하단 탭바와 안 겹치게 푸터 자체에 하단 여백 처리
- **주요 버튼**: 배경 `--accent`/hover `--accent-hover`/disabled `--border`+`--ink-soft` 텍스트. 보조 버튼(취소 등)은 무채색 아웃라인 유지. 파괴적 액션(삭제/차단/탈퇴)은 accent와 구분되는 빨간색(`red-500`) 유지 — 의도적 결정
- **링크**: 기본 상태는 `--ink-soft`, hover 시에만 `--accent`로 전환(항상 accent로 칠하지 않음 — "90% 무채색" 원칙 우선)
- **내비게이션 활성 상태 (2026-08-05, `bc1899b`로 전면 교체 — 위 2026-08-02 규칙은 폐기됨)**: 데스크톱 좌측 내비(`WorkspaceFrame`)가 플랫 구조로 바뀌면서 규칙도 단순해짐 — 항목별 색상 구분(accent vs ink) 없이, 활성 항목은 전부 동일하게 `bg-black/[0.04]` 옅은 배경 + `font-bold text-ink` + 왼쪽 2px `bg-ink`(accent 아님, 무채색) 라인. 모바일 `MobileTabBar`는 이 교체 대상이 아니라 기존 `text-accent`(활성)/`text-ink-soft`(비활성) 색상 기반 규칙을 그대로 유지 — 데스크톱과 모바일의 활성 표시 규칙이 서로 다른 상태(의도적으로 통일하지 않음)
- **로고**: 두 가지 버전이 공존 — (1) `/home` 페이지 전용 헤더(`EditorialHome.tsx`)는 데스크톱 86px / 모바일 36px 세리프(serif) 대형 로고, 절대 위치로 완전 중앙 정렬(2026-08-05, `2bc6652`로 예전 버전에서 복원). (2) 그 외 전 라우트가 쓰는 `WorkspaceFrame`/`HomeHeader`의 상시 헤더는 24px 세리프 로고, 좌측 햄버거 옆에 나란히 배치(중앙 정렬 아님). 표기는 "The PMPO"(2026-08-05, `cbdcedc`에서 "THE PMPO" 전체 대문자 표기를 이 형태로 수정). 텍스트 전체 `--ink`/`text-ink` 단색, 코랄 포인트 요소는 여전히 미구현 — 백로그 참고
- **프로필 메뉴 (2026-08-05, `d32dcc4` 신규)**: 헤더 우측의 아바타/닉네임을 누르면 열리는 드롭다운(`app/components/ProfileMenu.tsx`, 네이티브 `<details>/<summary>` 사용 — 별도 JS 상태 없이 열림/닫힘 처리). 마이페이지/관리자 페이지(관리자만)/로그아웃 3항목, `WorkspaceFrame`과 `HomeHeader` 양쪽 헤더가 공용으로 재사용
- **폐기된 시그니처 요소**: "질문있어요" 태그의 포스트잇 모서리 접힘(clip-path) 형태는 v1.1에서 배경을 없애면서 함께 제거됨(배경 없이는 접힌 모서리가 보이지 않으므로) — 이제 아이콘(🙋)+question색 텍스트만 사용

전체 화면별 고정 이미지는 `wireframe_images/` 폴더 참고(색상은 v1.0 기준이므로 톤 참고용, 실제 색상은 위 v1.1 토큰이 기준). **현재 MVP 속도 우선으로 진행 중이며, 세부 디자인(마이크로 인터랙션, 여백 등)은 이후 지속 개선 예정.**

---

## 5. 현재 개발 상태 & 블로커

- Next.js + Supabase(PostgreSQL, Auth) + Vercel 배포 환경 구축됨, `posts` 테이블 CRUD 구현됨, Vercel 배포 완료
- **해결됨(2026-07-29)**: 로그인 연동 마무리 — 로그인 성공 시 Supabase 세션을 통해 실제 유저 정보를 가져오도록 수정, 게시글 상세의 작성자 판별도 `user.id === post.user_id` 비교로 전환, 글쓰기 시 작성자도 실제 로그인 유저 정보로 기록되도록 수정
- **로그인 방식**: 이메일+비밀번호로 확정 (기존 코드에 매직링크(OTP) 방식으로 잘못 구현되어 있었음, 화면설계와 불일치 발견 후 수정)
- Supabase Auth의 Site URL 설정은 완료된 상태로 보임 (Authentication → URL Configuration에 배포 URL 등록)
- 로컬 개발 환경에 `.env` 파일이 없어 Vercel 환경변수에만 의존 중 — 로컬에서 실행하려면 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` 설정 필요
- **해결됨(2026-07-29)**: 회원가입 화면(화면 6) 구현 완료 — 이메일/비밀번호/닉네임 입력, 약관 동의 UI(전체동의 + 필수3 + 선택1), 닉네임/이메일 중복 검증, 비밀번호 8자 이상 검증. 닉네임 저장을 위해 `profiles` 테이블, 약관 동의 시각 기록을 위해 `user_agreements` 테이블 신설 (마이그레이션: `supabase/migrations/0001_profiles_and_user_agreements.sql`, `0002_handle_new_user_trigger.sql` — 둘 다 실행 완료)
  - 이메일 확인이 필요한 프로젝트라 가입 직후엔 로그인 세션이 없어서, 닉네임/약관 데이터를 auth 메타데이터로 같이 보내고 DB 트리거(`handle_new_user`)가 `profiles`/`user_agreements`에 자동으로 채워넣는 방식으로 구현 (RLS를 우회해야 하는 타이밍 문제라 트리거가 필요했음)
  - 실제 이메일 발송까지 걸친 종단 테스트는 Supabase 무료 플랜의 시간당 이메일 발송 한도에 걸려 아직 완료 못함 — 한도 초기화 후 재검증 필요
- 이용약관/개인정보처리방침 "보기" 링크는 실제 문서가 없어 임시로 "준비 중" 안내만 뜸 — 아래 우선순위 법적 문서 페이지 작업 시 연결 필요
- 로컬 개발 환경 `.env.local` 설정 완료, 로컬 개발 서버로 테스트 가능
- **해결됨(2026-07-29)**: `posts` 테이블 RLS 설정 완료 — 읽기는 누구나, 쓰기/수정/삭제는 본인 글만 가능. 익명 쓰기 시도가 401로 차단되는 것, 읽기는 계속 되는 것 직접 검증 완료 (`supabase/migrations/0003_posts_rls.sql`). 게시글 목록 화면의 수정/삭제 버튼도 작성자 본인에게만 보이도록 함께 수정 (기존엔 아무나 볼 수 있었음)
  - 트러블슈팅: 처음 RLS를 걸었는데도 익명 쓰기가 계속 뚫렸던 적이 있었음 — 원인은 예전에 이미 만들어져 있던 "전체 허용" 정책들(`allow_insert`, `insert_all`, `update_all`, `delete_all` 등, 이름이 `update_own_post`/`delete_own_post`라도 조건이 `true`라 사실상 전체 허용)이 남아있었기 때문. Postgres RLS는 같은 종류 정책을 OR로 합치므로, 새 테이블에 RLS를 걸 때는 항상 `pg_policies`로 기존 정책을 먼저 확인해야 함
  - 주의: 기존 `posts` 더미 데이터는 `user_id`가 비어있거나(`null`) 존재하지 않는 값(`"test-user-123"`)이라, 실제 유저와 매칭이 안 돼 앱에서 수정/삭제 불가능한 상태로 남아있음. 정리하려면 Supabase 대시보드 Table Editor에서 직접 삭제 필요
- **해결됨(2026-07-29)**: 커뮤니티 게시글 "질문있어요" 태그 기능 구현 완료 (PRD Must-have 1). `posts.is_question` 컬럼 추가·실행 완료(`supabase/migrations/0004_posts_is_question.sql`). 글쓰기 폼에 체크박스 추가, 목록/상세 화면에 앰버 포스트잇 스타일 뱃지로 표시, 목록 화면에 "🙋 질문만 보기" 토글 필터 추가(화면설계 화면1 기준 — PRD의 "질문 태그 + 댓글 없는 글만" 조합 필터는 댓글 기능 구현 후 보완 예정)
- **해결됨(2026-07-29)**: 커뮤니티 댓글 기능 구현 완료. `comments` 테이블 신설 + RLS 적용(읽기는 누구나, 쓰기/삭제는 본인만) — 익명 쓰기 401 차단, 읽기 200 정상 직접 검증 완료 (`supabase/migrations/0005_comments.sql`). 게시글 상세 화면에 댓글 목록/작성/본인 댓글 삭제 UI 추가, 비로그인 시 입력창 비활성화 + 안내 문구, 댓글 0개일 때 "아직 답변이 없어요. 첫 답변을 남겨보세요" 빈 상태 문구 (화면설계 화면2 스펙 반영). 1depth 고정(대댓글 없음), 댓글 좋아요는 다음 순서인 "좋아요" 기능에서 함께 구현 예정
  - 트러블슈팅: 게시글 상세 페이지가 비로그인이면 무조건 `/login`으로 리다이렉트되는 기존 버그 발견 — IA 문서상 게시글 상세는 비로그인도 열람 가능해야 하는데(쓰기 행동만 로그인 필요) 위반 상태였음. 댓글 비로그인 열람을 구현하려면 필수적으로 같이 고쳐야 해서 이번에 수정함
- **해결됨(2026-07-29)**: 좋아요 기능 구현 완료 (PRD Must-have 3, 화면설계 화면2 기준). 게시글/댓글 공용 `likes` 테이블 신설(`post_id`/`comment_id` 중 하나만 채워지는 구조, 유저당 대상 하나에 1개만 좋아요 가능하도록 부분 유니크 인덱스 적용) + RLS(읽기는 누구나, 쓰기/삭제는 본인만) — `supabase/migrations/0006_likes.sql` 실행 완료. 게시글 상세에 좋아요 버튼(❤️/🤍 토글, 클릭 시 +1, 재클릭 시 취소, 비로그인 시 로그인 페이지 이동), 댓글마다 좋아요 버튼 추가
- **해결됨**: 팔로우 기능 구현 완료 (PRD Must-have 5). `follows` 테이블 신설(`supabase/migrations/0007_follows.sql`), 게시글/아티클 상세의 작성자 닉네임 옆에 팔로우/팔로잉 토글 버튼 추가
- **해결됨**: 게시글 이미지 첨부 기능 구현 완료 (PRD Must-have 6). Supabase Storage 연동(`supabase/migrations/0008_post_images.sql`), 글쓰기 시 이미지 업로드, 목록에 썸네일 노출
- **해결됨**: 정보게시판(관리자 발행 아티클) 구현 완료 — `articles` 테이블(`supabase/migrations/0009_articles.sql`), 카테고리 탭(프로덕트/PM·PO트렌드/AI), 좋아요/댓글은 커뮤니티와 동일 규칙 공유, 조회수는 로그인 유저=user_id/비로그인=익명 쿠키 기준 24시간 중복 제외(`0011_article_views.sql`)
- **보안 패치**: `profiles.is_admin` 셀프 승격 방지 — 로그인 유저가 클라이언트에서 직접 자신의 `is_admin`을 바꿀 수 있던 구멍을 트리거로 차단, SQL Editor(비-JWT 세션)에서만 변경 가능하도록 제한 (`supabase/migrations/0010_prevent_self_admin_escalation.sql`)
- **해결됨**: 회원 탈퇴 기능 구현 완료 (화면9) — `delete_own_account()` RPC(SECURITY DEFINER)로 본인 탈퇴 처리, 정책대로 닉네임/게시글/댓글은 유지하고 프로필만 정리 (`supabase/migrations/0012_account_deletion.sql`)
- **해결됨(2026-07-30)**: 신고/차단/키워드 필터 등 리스크 대응 기능 구현 완료 (PRD "리스크 및 대응" 1~3번). `profiles.is_blocked` 컬럼 + 관리자 전용 `set_user_blocked()` RPC, `reports` 테이블(게시글/댓글/아티클 공용, 유저는 본인 신고만 등록·조회/삭제는 관리자만), `banned_keywords` 테이블, `posts`/`comments` BEFORE INSERT 트리거로 차단된 유저의 글쓰기 차단 + 금지 키워드 포함 시 등록 차단(서버단 강제, 클라이언트 우회 방지) — `supabase/migrations/0013_reports_and_moderation.sql`. 게시글/아티클 상세와 댓글에 🚨 신고 버튼 추가, `/admin` 관리자 페이지 신설(신고 목록 검토·콘텐츠 삭제·작성자 차단, 차단 유저 목록 관리, 금지 키워드 관리)
  - 트러블슈팅(2026-07-30): 개발 중 `git stash`가 생성됐다가 유실되는 사고로 로그인/홈/게시글상세 페이지와 이 문서의 상당 부분이 커밋 전 상태로 되돌아갔던 사건 발생 — git의 아직 gc되지 않은 dangling commit에서 복구하고 커밋으로 영구 보존함. 신고 버튼 등 유실됐던 이번 세션 작업도 함께 재적용
  - 버그 수정(2026-07-30): 라이브 테스트 중 관리자 페이지 "작성자 차단" 버튼이 항상 `is_blocked은 클라이언트 요청으로 변경할 수 없습니다` 에러로 실패하는 것 발견 — 0013이 0010의 "클라이언트발 변경 전면 차단" 트리거 패턴을 그대로 복사했는데, 0010과 달리 0013은 관리자가 앱에서 직접 호출하는 `set_user_blocked()` RPC가 있어서 정상 요청까지 막혔던 것. RPC 내부에서만 켜지는 트랜잭션 로컬 플래그로 우회하도록 수정 (`supabase/migrations/0014_fix_set_user_blocked_trigger.sql`). 신고 등록→관리자 열람→작성자 차단→차단된 유저 글쓰기 차단→금지 키워드 등록→키워드 포함 글쓰기 차단까지 전체 플로우 라이브 검증 완료

- **해결됨(2026-07-30)**: 법적 문서 페이지 자리 마련 완료 — `/terms`(이용약관), `/privacy`(개인정보처리방침) 페이지 신설, 회원가입 화면의 "보기" 링크를 placeholder alert에서 실제 페이지 링크(새 탭)로 연결. 실제 조항 문구는 법률 검토가 필요한 영역이라 임의 작성하지 않고, 조항 구조(제1조~제4조 등)와 "준비 중" 안내만 배치한 UI/UX 자리 — 문구 확정 시 내용만 채우면 됨
- **해결됨(2026-07-30)**: 회원가입→로그인 실제 UI 종단 테스트 완료(REST 세션 주입 우회 없이) — 테스트 계정으로 `/signup` 가입 후 `/login`으로 정상 로그인·세션 발급·`handle_new_user` 트리거의 `profiles` 자동 생성까지 확인. 더미 `posts` 데이터는 재확인 결과 현재 2건 모두 유효한 `profiles.id`와 연결되어 있어 별도 정리 불필요(과거 이슈였던 빈 값/가짜 `user_id` 레코드는 이미 없음)
- **버그 수정(2026-07-30)**: 배포 전 체크리스트 점검 중 정보게시판(articles) 관리자 삭제 권한 누락 발견 — 0013에서 `posts`/`comments`에는 "관리자는 누구나 삭제 가능" 정책을 추가했지만 `articles`는 빠뜨려서, 신고된 글을 작성자 본인이 아닌 다른 관리자가 삭제 시도하면 에러 없이 조용히 0건 삭제되는 문제 있었음(라이브 테스트로 재현). posts/comments와 동일한 관리자 오버라이드 정책 추가로 수정 (`supabase/migrations/0015_admin_delete_any_article.sql`), 다른 관리자 계정으로 삭제 재검증 완료

### 정보게시판(정보게시판/articles) 완성도

PRD/IA 요구사항 대비 기능 구현 완료: 카테고리 탭(프로덕트/PM·PO트렌드/AI), 관리자 전용 발행(이미지 첨부 포함), 좋아요/댓글은 커뮤니티와 동일 규칙 공유, 조회수 로그인/비로그인 구분 집계(24시간 중복 제외), 신고 및 관리자 삭제(0015로 권한 버그 수정 완료). 추가 기능 요구사항 없음 — 남은 항목은 아래 우선순위의 콘텐츠/운영성 작업뿐.

- **해결됨(2026-07-30)**: 법적 문서(`/terms`, `/privacy`) 실제 조항 문구 작성 완료 — 이메일+비밀번호 가입, 닉네임, 이미지 업로드, 신고/차단, 회원탈퇴 정책(닉네임 유지/게시물 보존) 등 이 서비스의 실제 기능에 맞춘 표준 조항으로 채움. 개인정보보호책임자(김상현 / thepmpo.official@gmail.com), 공고일자·시행일자(2026-08-02, 사이트 오픈 예정일 기준)까지 기입 완료, 이용약관에도 동일 시행일로 부칙 조항 추가. 단, AI가 작성한 초안이며 변호사 등 법률 전문가 검토를 거치지 않은 상태 — 페이지 상단에 "초안, 검토 필요" 안내 문구 유지 중. **개발 관점에서는 완료, 배포 전 법률 검토만 남음(운영자 액션)**
- **해결됨(2026-07-30)**: 커뮤니티 목록(화면1) "인기 댓글 미리보기" 구현 완료 — 각 게시글 카드 하단에 좋아요 상위 2개 댓글을 작성자·좋아요 수와 함께 노출(좋아요 0개인 댓글은 제외). 게시글 목록 조회 시 `comments`+`likes`를 post_id 기준으로 조회해 댓글별 좋아요 수를 집계 후 정렬하는 방식으로 구현(`app/page.tsx`). 라이브로 좋아요 2개/1개/0개 댓글 3개를 생성해 상위 2개만 좋아요 수 내림차순으로 노출되는 것 확인 후 테스트 데이터 정리 완료
- **해결됨(2026-07-30)**: 커뮤니티 목록(화면1) PRD Must-have 4 "'질문' 태그된 글 중 댓글 없는 것만 보기" 조합 필터 구현 완료 — 기존 "질문만 보기" 토글과 별개로 "🙋 답변 없는 질문만" 토글 추가, `is_question === true && 댓글 수 === 0` 조건으로 필터링(`app/page.tsx`). 라이브로 답변없는질문/답변있는질문/질문아닌글 3종 생성해 토글 시 답변없는질문만 노출되는 것 확인 후 테스트 데이터 정리 완료 — **화면1 PRD 요구사항 전부 구현 완료**
- **해결됨(2026-07-31)**: 사이트 전체 공용 내비게이션 + UI 디자인 시스템 실제 적용 완료 — `app/components/Sidebar.tsx`(커뮤니티/정보게시판 메뉴, 마이페이지 진입용 프로필 카드), `app/components/AppShell.tsx`(로그인/회원가입만 사이드바 없는 독립 카드, 나머지는 좌측 사이드바+본문 레이아웃) 신설. `PMPO_UI디자인시스템_v0.3.md`/`wireframe_images` 기준 컬러 토큰(Paper/Surface/Ink/Cobalt/Amber/Sage), Noto Sans KR 폰트, 카드 그림자, "질문있어요" 포스트잇 clip-path를 `globals.css`/`layout.tsx`에 등록하고 커뮤니티·정보게시판·로그인/회원가입·마이페이지·관리자·약관/개인정보처리방침까지 전 페이지의 inline style을 교체(기능 로직 변경 없음). 그 전까지는 기능만 구현되고 디자인은 전혀 입혀지지 않은 상태였음
- **해결됨(2026-07-31)**: 도메인(`thepmpo.com`) 전파 완료 — 네임서버 전파, Vercel A 레코드 발급, HTTPS 접속(200 OK) 전부 확인. Supabase Auth의 Site URL을 `https://thepmpo.com`으로 갱신, Redirect URLs에 `https://thepmpo.com/**` 추가(기존 `myapp-taupe-seven.vercel.app`도 유지) 완료 — **배포 전 도메인/인증 URL 작업 전부 종료**
- **해결됨(2026-07-31)**: 회원가입 이메일 발송 종단 테스트 완료 + 커스텀 SMTP(Resend) 연동 완료. 실제 원인은 기존에 추정했던 "시간당 발송 한도"가 아니라 두 가지였음: (1) Supabase Auth의 "Confirm email" 옵션이 꺼져 있어서 확인 메일 발송 자체가 시도되지 않고 가입 즉시 자동 확인 처리되던 버그 발견 → 다시 켬. (2) 다시 켜고 나니 Supabase 기본 메일 발송이 "프로젝트 소유자 본인 이메일로만" 테스트 발송을 허용하는 샌드박스 제한이라 실제 가입자에게는 영원히 발송이 불가능한 구조였음(로그: `gomail: could not send email... You can only send testing emails to your own email address`). Resend에 `thepmpo.com` 도메인을 추가하고 DKIM/SPF/DMARC/MX 레코드를 Vercel DNS에 등록해 도메인 인증 완료, Supabase Auth SMTP 발신 주소를 샌드박스용 `onboarding@resend.dev`에서 인증된 `noreply@thepmpo.com`으로 교체. 실제 가입 → 확인 메일 수신 → 링크 클릭 → 로그인 세션 생성까지 라이브로 전체 플로우 검증 완료(발신자가 `noreply@thepmpo.com`으로 정상 표시됨) — **회원가입 플로우 배포 전 마지막 블로커 해소**
- **해결됨(2026-08-01)**: 로컬 `.env.local`과 Vercel 환경변수 값 일치 확인 완료 — `NEXT_PUBLIC_SUPABASE_URL`은 동일했지만, `NEXT_PUBLIC_SUPABASE_ANON_KEY`는 서로 다른 상태였음(로컬은 Supabase 최신 포맷인 `sb_publishable_...` 키, Vercel은 예전 방식의 legacy JWT anon key). 둘 다 현재는 정상 동작하지만 Supabase가 향후 legacy 키를 만료시키면 Vercel 배포판만 인증이 끊길 위험이 있어, Vercel 쪽 값을 로컬과 동일한 `sb_publishable_...` 키로 교체하고 재배포 완료. 배포 후 라이브 사이트에서 데이터 정상 로드 확인 — **배포 전 환경변수 점검 항목 종료**
- **해결됨(2026-08-01)**: UI 디자인 시스템(v0.3) 적용 현황 감사 및 잔여 항목 정리 완료. 로그인/회원가입/마이페이지/내정보수정/팔로워목록 5개 화면 코드를 토큰 기준으로 하나씩 점검한 결과, 대부분은 이미 적용되어 있었고 실제로 빠졌던 부분만 수정: 회원가입 메인 폼에 "The PMPO" 브랜드 표기 추가(기존엔 성공 화면에만 있었음), 프로필 뱃지의 🏅 이모지를 stroke SVG 아이콘으로 교체, 프로필/팔로워목록의 아바타 placeholder 색을 임의색(`bg-black/10`)에서 토큰(`bg-border`)으로 통일. 감사 중 두 가지가 추가로 발견/처리됨: (1) **비밀번호 재설정 화면이 아예 구현된 적 없었음** — `/reset`(재설정 메일 요청, 화면7 와이어프레임 그대로)과 `/reset/confirm`(메일 링크로 진입해 새 비밀번호 설정) 2개 화면을 디자인 토큰 100% 적용해 신규 제작, 로그인 화면에 "비밀번호를 잊으셨나요?" 링크 연결, `AppShell`에 사이드바 없는 독립 라우트로 등록. 도메인/SMTP 설정은 건드리지 않고 기존 Resend 발송 인프라를 그대로 재사용. (2) **회원 탈퇴 확인이 브라우저 기본 `window.confirm()`이었음** — "되돌릴 수 없음" 경고 문구 + 취소/탈퇴하기 버튼을 갖춘 커스텀 모달로 교체(`app/profile/edit/page.tsx`). 커뮤니티 목록의 "미답변" 필터 로직은 재확인 결과 이미 올바르게 구현되어 있어 별도 수정 불필요. 실제 가입→이메일 확인→로그인→탈퇴 모달까지 라이브로 전체 플로우 검증 완료, `tsc`/`next build` 정상 통과 — **디자인 시스템 v0.3 적용 마무리**
- **해결됨(2026-08-01)**: `/reset` 신규 구현 후 실제 이메일 종단 테스트 중 이메일 콘텐츠 문제 발견 및 수정. `resetPasswordForEmail`에 넘긴 `redirectTo`(로컬/커스텀 경로)가 Supabase Auth의 Redirect URLs 허용 목록에 없으면 조용히 Site URL로 대체된다는 점을 확인(운영 도메인 `https://thepmpo.com/**`는 이미 허용되어 있어 실제 서비스에서는 정상 동작 — 로컬 테스트에서만 겪는 제약). 더 중요한 발견: Supabase 기본 이메일 템플릿의 영어 문구("Follow this link to reset the password for your user")가 Gmail의 피싱 탐지에 걸려 **"위험한 메일일 수 있음" 경고 배너**와 함께 수신됨(SPF/DKIM/DMARC는 전부 정상 통과 — 인증 문제 아님, 순수 문구 문제). Supabase Auth → Emails → Templates에서 "Confirm sign up"과 "Reset password" 두 템플릿의 제목/본문을 한글 브랜드 톤으로 교체(`[The PMPO] 회원가입 이메일 확인`, `[The PMPO] 비밀번호 재설정 안내`). 수정 후 재발송 테스트로 경고 배너가 사라지는 것 확인 — **회원가입/비밀번호 재설정 이메일 모두 정상 수신 확인**
- **해결됨(2026-08-01)**: 정보게시판 카테고리 탭 디자인 확정 및 3개 핵심 유저 플로우(주니어 PM/PM 취업 희망자/시니어 PM) 기능 QA 진행, 발견된 버그 전부 수정 완료.
  - 카테고리 탭(`app/articles/page.tsx`): 색상 토큰(Cobalt 강조/Ink Soft 비활성)은 이미 정상 적용되어 있었으나, 선택된 탭에만 `font-medium`이 붙어 탭 전환 시 텍스트 폭이 변하며 좌우로 미세하게 밀리는 레이아웃 시프트 발견 → 두 상태 모두 `font-medium` 고정, 색상만으로 구분하도록 수정
  - **[치명적] 커뮤니티 목록에 게시글 상세로 가는 링크가 전혀 없었음** — 게시글 카드 제목이 클릭 불가능한 `<div>`라 사용자가 URL을 직접 입력하지 않는 한 댓글/좋아요/팔로우가 있는 상세 화면에 진입할 방법이 UI상 없었음(3개 유저 플로우 전부가 이 화면 전환에 의존). `app/page.tsx`에서 제목을 `<Link href={/post/${post.id}}>`로 교체하여 수정
  - **[개인정보 노출] 게시글/댓글 작성자 표시가 닉네임이 아닌 실제 이메일 주소로 노출되고 있었음** — 프로필 페이지·사이드바는 정상적으로 닉네임을 표시했지만 커뮤니티 목록·게시글 상세·댓글 작성자 표시만 `posts.author`/`comments.author`(가입 시 저장된 이메일 값)를 그대로 렌더링하던 상태. `app/articles/page.tsx`에 이미 있던 `profiles` 조인 패턴(`fetchNicknames`)을 `app/page.tsx`, `app/post/[id]/page.tsx`에도 동일하게 적용해 `user_id` 기준으로 닉네임을 조회해 표시하도록 수정, 원본 이메일 컬럼(`author`)은 데이터 보존 목적으로 그대로 유지(닉네임 없을 때만 폴백)
  - 3개 유저 플로우 라이브 QA 결과: "답변 없는 질문만 보기" 필터, 질문 등록, 댓글 등록/삭제, 게시글·댓글 좋아요(토글/카운트), 팔로우(토글/팔로워 수 갱신) 전부 정상 동작 확인. 정보게시판 콘텐츠 미발행(운영 이슈, 코드 문제 아님)과 커뮤니티 검색 기능 부재(PRD Must-have 아님)는 버그가 아닌 별도 항목으로 분류, 이번엔 수정하지 않음
  - QA 중 생성한 테스트 계정(qauserA/qauserB) 및 관련 게시글·댓글·좋아요·팔로우 데이터는 검증 완료 후 전부 삭제 완료
- **해결됨(2026-08-02)**: 컬러 시스템을 v1.0(다색 팔레트)에서 v1.1(색상 절충안, `docs/PMPO_컬러시스템_v1.1_확정본.md` 기준)로 전면 재조정 완료. 레이아웃/타이포그래피/컴포넌트 구조는 그대로, 컬러 값과 "색을 쓰는 방식"만 교체 — 단계별로 사용자 확인받으며 진행.
  - **1단계(스캔)**: 배경색이 채워진 칩/배지/활성상태 8곳(질문 태그 2곳, 질문 카드 좌측 보더 2곳, 필터 버튼 2곳, 사이드바 활성 메뉴 2곳) 발견. 요청 범위 밖이지만 같은 성격으로 추가 발견된 3곳(프로필 뱃지, 아티클 카테고리 라벨 칩, 약관/개인정보처리방침 안내 배너)도 사용자 승인 하에 함께 전환
  - **2단계(칩→표식 전환)**: 위 11곳 전부 배경 채움 제거 → 아이콘/텍스트/얇은 라인만 남기는 방식으로 전환. `globals.css`에 v1.1 무채색 토큰(ink/ink-soft/paper/border 새 값) + accent/accent-hover/question/error 신규 토큰 추가(구 토큰은 3단계 완료 후 삭제). "질문있어요" 태그의 포스트잇 모서리 접힘(clip-path) 시그니처 요소는 배경 제거에 따라 자연 폐기(배경 없이는 안 보이는 형태라서). 사이드바 활성 표시는 배경 틴트→왼쪽 2px 라인+볼드로 전환하며 레이아웃 시프트 방지 처리(투명 보더로 폭 유지)
  - **3단계(버튼/링크)**: v1.0 색상(cobalt/amber/sage)을 쓰던 파일 14개 전체를 accent/accent-hover로 전환. 주요 버튼 disabled 상태를 기존 `opacity-60`(단순 흐리게)에서 `bg-border`+`text-ink-soft`(명시적 무채색)로 변경. 파괴적 액션(삭제/차단/탈퇴) 버튼은 accent로 바꾸지 않고 기존 빨간색 유지(경고색 구분 목적, 의도적 결정). 전환 완료 후 `globals.css`에서 미사용 v1.0 토큰(cobalt/amber/sage) 전부 삭제
  - **4단계(로고 확인)**: 컬러 문서상 "기존 결정(B안) 유지: 로고는 코랄 포인트 요소 포함"이라고 되어 있었으나, 실제 코드(`app/components/Sidebar.tsx`)에는 순수 텍스트만 있고 코랄 포인트가 구현된 적이 없었음을 발견(관련 로고 파일도 없음) — 즉 "B안"이 기획 단계에서만 존재하고 코드에는 반영된 적 없는 상태. 사용자 확인 결과 현재 상태(포인트 없음) 그대로 유지하기로 결정, 추가는 백로그로
  - **5단계(전체 감사)**: 코드 전수 조사 결과 남은 채워진 배경은 `bg-accent`(주요 버튼 13곳), `bg-red-500`(파괴적 액션 5곳), `bg-accent/5`(hover 틴트 1곳)뿐 — 의도되지 않은 칩/배지 0곳 확인. 링크 텍스트는 원래 요청("기본부터 accent")과 달리 기존 hover-only 패턴(기본 ink-soft, hover 시만 accent)을 유지하기로 사용자와 협의 — "90% 무채색" 원칙에 더 부합한다고 판단
  - `tsc`/`next build` 정상 통과, 로컬에서 라이브로 색상 값(computed style)까지 지정한 hex와 정확히 일치하는 것 확인
- **해결됨(2026-08-02, 로컬 완료 · 커밋/배포 전)**: "커뮤니티 → Circle", "정보게시판 → Insights" 명칭 변경 + Insights 사이드바 2depth 구조 개편(`docs/PMPO_IA_v0.3.md`/`화면설계_v0.8.md`/`톤앤보이스_v0.2.md` 기준). 5단계로 나눠 사용자 승인받으며 진행.
  - **명칭 변경**: `app/page.tsx`(제목 "Circle" + 소개 카피 추가), `app/admin/page.tsx`(신고 타입 라벨·뒤로가기 링크), `app/terms/page.tsx`/`app/privacy/page.tsx`(단, 문서 안에서 "커뮤니티"가 특정 메뉴가 아니라 서비스 전반을 가리키는 일반 명사로 쓰인 문장은 의미가 달라지지 않게 그대로 둠 — 예: 제1조 "PM/PO 커뮤니티 서비스", privacy 2번 "건전한 커뮤니티 운영")
  - **사이드바(`app/components/Sidebar.tsx`) 재구성**: Circle·Insights를 동일 비중의 1depth로, Insights 아래 Product/Trends/AI를 상시 노출 2depth로 추가. Active 표시는 위 "UI 디자인 시스템" 절의 신규 규칙(1depth Insights=라인 없이 볼드만, 2depth=라인+볼드) 적용
  - **라우팅 이관**: `app/articles/page.tsx`(카테고리 탭 포함 단일 페이지) → `app/insights/product|trend|ai/page.tsx`(카테고리별 고유 라우트, 탭 UI 제거, 서버 쿼리에서 바로 `.eq("category", ...)` 필터링) + 공용 `app/insights/_components/ArticleListPage.tsx`. 상세는 `app/articles/[id]/page.tsx` → `app/insights/[id]/page.tsx`로 이관(뒤로가기는 어느 카테고리에서 왔는지 추적하지 않고 `router.back()`으로 처리). 관리자 글쓰기 폼은 카테고리 페이지에서 분리해 `app/insights/new/page.tsx`로 단일화(카테고리는 폼 안 드롭다운으로 선택), 카테고리 목록 페이지엔 관리자에게만 보이는 "+ 새 글 작성" 진입 버튼 추가(안 넣으면 관리자가 글쓰기 화면에 들어갈 방법이 없었음). 카테고리 라벨 맵은 `app/lib/insightsCategories.ts`로 공용화(기존 두 파일에 중복 정의돼 있던 것 정리)
  - `app/articles/` 디렉토리 전체 삭제, 리다이렉트는 추가하지 않음(정식 오픈 전이라 외부 노출 링크 없다고 판단)
  - `tsc --noEmit`/`next build` 통과 확인 후, 로그인한 관리자 계정으로 실제 글 발행(Trends 카테고리) → 목록 노출 → 상세 페이지(카테고리 라벨/조회수/좋아요/댓글 정상) → 뒤로가기 → 삭제까지 라이브 종단 테스트 완료. 테스트 글은 Article 삭제 UI가 없어(신고 경유 삭제만 가능하고 본인 글은 신고 불가) Supabase REST API를 현재 세션의 access token으로 직접 호출해 삭제, 데이터 정리 완료
  - 커밋(`1a91ce5`) 후 `origin/main` 푸시, Vercel 자동 배포 완료 확인 — `thepmpo.com/insights/product` 200, 예전 `thepmpo.com/articles` 404, 운영 사이트에서 Circle/Insights 사이드바·소개 카피·2depth 카테고리 라이브 반영까지 확인
- **해결됨(2026-08-02, 로컬 완료 · 커밋 전)**: 모바일 웹 내비게이션 구조 신규 구현(네이티브 앱 아님, 반응형 CSS 분기) — 상세는 위 "모바일 웹 레이아웃 규칙" 절 참고. `app/components/icons.tsx`(아이콘 공용화) 신규, `app/components/MobileTabBar.tsx` 신규, `AppShell.tsx`(Sidebar `lg` 이상만 노출 + MobileTabBar 추가 + 본문 하단 패딩), `Sidebar.tsx`(아이콘 import로 전환), `ArticleListPage.tsx`(모바일 전용 가로 스크롤 카테고리 탭 추가). 로컬에서 모바일 뷰포트(390px)·데스크톱 뷰포트(1400px) 둘 다 스크린샷으로 직접 확인, `tsc`/`next build` 통과
- **해결됨(2026-08-03)**: 레이아웃 재구조화 + 카드 디자인 + 전역 푸터 + 여러 기능 추가(대규모 작업, 배포까지 완료).
  - **레이아웃/카드/푸터**: 상세는 위 "UI 디자인 시스템" 절 참고. 사이드바 고정+세로 구분선+본문 `mx-auto` 중앙정렬(gap 고정값 방식 폐기), 목록 카드 박스→구분선+hover 라인 전환, 전역 다크 푸터 신규, 본문 최대폭 2배 확대(1360/1280px) 과정에서 `flex justify-center` 방식의 중앙정렬 버그 발견 후 `mx-auto` 방식으로 재수정(자세한 원인은 위 "구현 함정" 참고)
  - **회원가입 강화**(`app/signup/page.tsx`): 비밀번호 확인란 추가, 비밀번호 규칙(8자+영문/숫자, `app/lib/passwordRules.ts`로 공용화) 신규 적용 후 `/reset/confirm`·`profile/edit` 비밀번호 변경에도 동일 규칙 통일 적용(재설정 경로가 더 약한 규칙으로 우회되는 구멍 방지), 이메일/닉네임 중복은 제출 후 인라인 에러로 전환(실시간 확인은 이메일 열거 공격 우려로 Supabase가 지원 안 함), 약관 전체 동의+유효성 통과해야 "가입하기" 버튼 활성화
  - **닉네임 월 1회 제한**: `profiles.nickname_changed_at` 컬럼 + BEFORE UPDATE 트리거로 서버단 강제(마이그레이션 `0016_nickname_change_limit.sql`, 0013의 `is_blocked` 보호 패턴과 동일 이유 — 클라이언트 비활성화만으로는 REST API 직접 호출 우회 가능). `profile/edit` UI에서 잠금 기간엔 입력란 비활성화+안내문구
  - **관리자 페이지 분리**: 기존 `/admin`(신고/유저/키워드)은 그대로 두고 `/admin/insights` 신설(목록+수정+삭제), 작성/수정은 `/admin/insights/new`·`/admin/insights/[id]/edit`(공용 `ArticleForm` 컴포넌트). `app/admin/layout.tsx` 신설로 관리자 권한 체크+서브탭 내비게이션을 한 곳에서 처리(기존엔 `/admin/page.tsx`에 직접 박혀있던 걸 분리). 기존 `app/insights/new`는 폐기하고 이관. 아티클 **수정** RLS가 "본인 글만"으로 남아있던 걸 발견해(삭제는 0015에서 이미 고쳤었음) `0017_admin_update_any_article.sql`로 삭제와 동일하게 "관리자면 누구나 수정 가능"으로 통일
  - **Circle 글쓰기 플로우 전면 개편**: 목록 페이지 상단 인라인 작성폼(제목+글쓰기 버튼) 제거, 전용 페이지 `app/circle/new/page.tsx` 신설(제목/본문/질문태그/이미지, Reddit 글쓰기 화면 참고). 이 과정에서 **`posts` 테이블에 애초에 `content`(본문) 컬럼이 없었다는 걸 발견**(0013 주석에 기록돼 있던 사실 — Circle 글은 지금까지 사실상 "제목만 있는 글"이었음) → `0018_posts_content.sql`로 컬럼 추가, 겸사겸사 금지어 필터 트리거(`enforce_post_moderation`)도 title만 검사하던 걸 content까지 검사하도록 갱신(안 고치면 본문으로 필터 우회 가능했음). 새로 생긴 본문/이미지는 Circle 목록 카드(미리보기 2줄+썸네일)와 상세 페이지(전체 본문+이미지)에도 Insights와 동일한 패턴으로 반영해 완결된 기능으로 마무리. "+ 글쓰기" 진입 버튼을 Insights의 "+ 새 글 작성"과 동일 위치·스타일로 Circle 목록에도 추가(로그인 유저 전체 대상, 데스크톱·모바일 공통 — 우측 프로모박스는 `lg` 미만에서 안 보이므로 모바일 진입점 확보 차원에서도 필요했음)
  - 마이그레이션 3개(`0016`~`0018`) 사용자가 Supabase에서 직접 실행, 실행 후 관리자 계정으로 Circle 글쓰기(제목+본문+질문태그) → 목록 미리보기 → 상세 전체 표시 → 삭제까지 라이브 종단 테스트 완료, 관리자 Insights 글 작성→수정→삭제 흐름도 별도로 라이브 검증 완료. 테스트 중 우연히 발견된 정체불명의 테스트성 글("ㄴㅇ리ㄴㅇ리" — 기존 seed 데이터로 추정)도 함께 정리
  - `tsc`/`next build` 통과, 커밋 후 배포 완료(정확한 커밋 해시는 아래 "다음 세션 시작 시 참고" 확인)
- **해결됨(2026-08-03)**: 프로필 아바타 업로드 기능 신규 구현(`profiles.avatar_url` 컬럼 + `avatars` Storage 버킷, `supabase/migrations/0019_avatars.sql`). Public 버킷이라 `getPublicUrl()`에는 SELECT RLS가 필요 없다는 점을 확인해 기존에 있던 "공개 읽기" 정책은 오히려 `list()`로 전체 오브젝트 목록이 노출될 수 있어 제거, 업로드/삭제만 `to authenticated` + 본인 폴더(`auth.uid()`) 조건으로 제한. 이후(`0a77e9c`) 업로드 시 서버단 크기/타입 검증 강화. 프로필 편집 화면에서 업로드 가능, 헤더/프로필 카드에 노출
- **해결됨(2026-08-03)**: `/home`(에디토리얼 랜딩, every.to 스타일 — 최신 Insights/인기글/Circle 인기글 노출)과 `/notice`(공지사항 자리만 마련된 placeholder) 라우트 신규 추가(`1c9570f`). 이 과정에서 `AppShell`의 빈 레이아웃 분기 때문에 `/profile/*`·`/circle/new`가 하얗게 비어 보이던 회귀 버그도 함께 발견해 수정
- **해결됨(2026-08-04, `57653b5`)**: Circle 게시글 상세를 로그인 필요로 정책 변경 — Circle 목록/Insights는 계속 완전 공개, 상세만 비로그인 시 로그인 유도 모달로 대체(목록 카드 클릭 단계 + 상세 라우트 자체 양쪽에서 강제해 직접 URL 공유로 우회 불가). 자세한 근거는 위 2장 "로그인 필요 여부" 절 참고 — **IA v0.3 문서의 기존 "비로그인도 열람 가능" 서술과 달라진 지점이라 이번에 문서를 코드 기준으로 갱신함**
- **버그 수정(2026-08-04, `b61ea60`)**: 아바타/Circle·Insights 이미지 업로드 시 파일명에 공백이나 한글이 포함되면(예: macOS 스크린샷 기본 파일명) Storage가 "Invalid key"/"No content provided" 에러로 실패하던 문제 발견 및 수정 — 원본 파일명 대신 안전한 확장자만 추출해 업로드 경로를 구성하도록 전면 교체. 같은 커밋에서 데스크톱 좌측 내비를 사이드바 2depth(Insights>Product/Trends/AI)에서 플랫 리스트(Home/Circle/Product/Trends/AI)로 되돌림 — 위 2·3장 참고
- **해결됨(2026-08-05, `bc1899b`)**: 기존 `Sidebar.tsx`를 완전히 폐기하고 `WorkspaceFrame.tsx` 하나로 전 라우트 내비게이션을 통합(`AppShell`이 라우트별로 다른 내비 컴포넌트를 분기하던 방식 제거). 관리자가 Circle 인기글을 직접 큐레이션할 수 있는 "추천글" 기능 신규 추가(`posts.is_featured` 컬럼, `supabase/migrations/0020_posts_featured.sql` — 0017과 동일한 이유로 관리자 UPDATE 오버라이드 RLS 정책 포함) + `/admin/featured` 관리 페이지, Home의 "지금 인기 있는" 영역이 기존 자동 랭킹 대신 이 큐레이션 목록을 사용하도록 변경. 신고 처리 플로우도 `confirm()`/`alert()` 브라우저 기본 다이얼로그에서 인페이지 모달+토스트로 교체
- **해결됨(2026-08-05, `d32dcc4`)**: 헤더 우측에 공용 `ProfileMenu` 드롭다운(마이페이지/관리자 페이지/로그아웃) 신규 도입, `/admin`을 신고·차단 관리(기존 `page.tsx`) + `circle`(Circle 모더레이션) + `featured`(추천글 관리) + `insights`(아티클 관리, 8/3에 먼저 분리됨) + `keywords`(금지어 관리, 기존 `/admin`에서 분리) + `stats`(가입 통계) + `answer-rate`(답변률 대시보드) 총 7개 탭으로 확장, `app/admin/layout.tsx`에서 관리자 권한 체크+탭 내비게이션을 한 곳에서 처리
- **해결됨(2026-08-05)**: 헤더 로고 스타일을 여러 차례 조정한 끝에 최종적으로 (1) `/home` 전용 헤더는 데스크톱 86px/모바일 36px 대형 세리프 로고로 복원(`2bc6652`), (2) 그 외 라우트의 상시 헤더(`WorkspaceFrame`/`HomeHeader`)는 24px 소형 세리프 로고 유지, (3) 표기를 "THE PMPO"에서 "The PMPO"로 통일(`cbdcedc`)로 정착 — 자세한 스타일은 위 4장 "로고" 절 참고
- **버그 수정(2026-08-05, `71a91ca`)**: 좌측 내비의 "팔로잉" 미리보기 목록이 라우트 이동마다 다시 fetch되며 깜빡이던 문제 발견 — 원인은 `AppShell`이 라우트마다 구조적으로 다른 트리를 렌더링해 `WorkspaceNavigation`이 매번 리마운트되던 것. 루트 레벨 `WorkspaceDataContext`(`WorkspaceDataProvider`)를 도입해 한 번만 fetch하고 라우트 전환 시에도 데이터를 유지하도록 변경 — 이후 비슷한 "여러 헤더/내비가 각자 다시 fetch"하는 패턴이 생기면 이 컨텍스트를 확장해서 재사용할 것
- **폴리시 패스(2026-08-05, 다수 커밋)**: Circle 게시글 수정 화면을 `/circle/new`와 동일한 레이아웃으로 재구축(`ab4b9aa`), 좋아요 아이콘을 이모지에서 공용 stroke SVG로 통일(`809a086`), 게시글/댓글 작성자 아바타를 Circle 목록·상세 전반에 노출(`f400484`, `83c9471`), "질문있어요" 태그 위치를 작성자 행 우측(팔로우 버튼 옆)으로 이동(`a2d8d61`), 모바일에서 인기 댓글 미리보기 숨김(`83c9471`), 본문 폭/여백/헤더 위치를 Circle·Insights·프로필 화면 간에 맞추는 다수의 정렬 수정(`71a91ca`, `1583670`, `87c3266`, `e2c0a38`, `531d898`) — 개별 항목은 기능 변경이 아닌 시각적 일관성 조정이라 커밋 목록으로만 남김

### 세션 핸드오프 (2026-08-06 작업 종료 시점 — 다음 세션은 여기부터 읽으면 됨)

**1. 이번 세션에서 완료한 작업**

두 갈래로 진행됨.

- **버그 수정 4건** (닉네임 중복체크, Circle 필터 빈 상태, 사이드바 팔로잉 미리보기 링크, 댓글 레이아웃) — 그중 2개(닉네임 중복체크, 댓글 레이아웃)는 라이브 확인 후 재요청을 받아 두 라운드에 걸쳐 수정. 상세:
  1. **닉네임 중복 체크가 안 되던 문제** (`app/signup/page.tsx`, `app/profile/edit/page.tsx`) — 처음엔 trim 처리 누락으로 오인했으나(1차 수정, 커밋 `3c163df`), 사용자가 스크린샷으로 재현: 실제 원인은 중복 체크 쿼리가 "가입하기"/"저장" 버튼 클릭(`handleSignup`/`saveNickname`) 안에서만 실행돼서, 비밀번호 확인란이 틀리는 등 다른 필드가 유효하지 않으면 버튼이 비활성화돼 닉네임 체크 자체가 실행되지 않았던 것. **최종 수정(커밋 `9ef815c`)**: 닉네임 입력란에 `onBlur` 핸들러를 달아 다른 필드 상태와 무관하게 독립적으로 중복 체크가 실행되도록 변경. 로컬에서 "비밀번호 확인 불일치 + 중복 닉네임" 조합으로 실제 재현 테스트해 정상 동작 확인(스크린샷 확보)
  2. **Circle "답변 없는 질문만" 필터** — 필터 로직 자체는 정상이었고, 결과가 0건일 때 안내 문구가 없어서 "안 되는 것처럼" 보였던 문제. 필터별 빈 상태 메시지 추가 (`app/page.tsx`, 커밋 `3c163df`)
  3. **사이드바 "팔로잉" 미리보기(최근 5명) 닉네임 클릭 시 프로필로 이동하던 문제** — `/profile/[userId]/follows` 목록 페이지와 동작을 맞추기 위해 클릭 불가능한 `<span>`으로 변경 (`app/components/home/WorkspaceFrame.tsx`, 커밋 `3c163df`)
  4. **댓글 레이아웃 순서** — 두 차례 스펙 변경을 거침. 1차(커밋 `3c163df`→`27918ad`)는 "아바타+닉네임+내용이 한 줄에 흐르다 넘치면 다음 줄로" 요청에 맞춰 `flex flex-wrap` 한 줄 레이아웃으로 구현했으나, 이후 사용자가 참고 이미지(Reddit 스타일)를 제시하며 **완전히 새로운 스펙으로 재요청**. **최종 구현(커밋 `9ef815c`, `app/post/[id]/page.tsx`)**: (1) 닉네임+작성 시간을 한 행("N시간 전"/"N일 전" 상대시간, `app/lib/relativeTime.ts` 신규), (2) 내용은 별도 행, (3) "공감해요"는 그대로 노출하고 "신고"는 "⋯" 오버플로 메뉴 뒤로 숨김(바깥 클릭 시 자동으로 닫힘)
- **CLAUDE.md 전면 감사·갱신** — 2026-08-03~08-06 사이 커밋됐지만 문서에 반영 안 돼 있던 작업들(Home/Notice 라우트, 아바타 업로드, `Sidebar.tsx`→`WorkspaceFrame.tsx` 내비게이션 통합, 관리자 페이지 7탭 분리, 관리자 큐레이션 추천글, **Circle 상세 로그인 필요 정책 변경**, 로고/프로필메뉴 등 UI 변경, 업로드 파일명 버그 수정, 팔로잉 리스트 리페치 버그 수정)을 커밋 로그 전수조사로 확인해 2장(IA)·3장(화면설계)·4장(UI 디자인 시스템)·5장(개발 상태)에 반영. 특히 2장 "로그인 필요 여부" 표와 3장 "데스크톱 레이아웃 규칙"은 실제 코드와 어긋나 있던 부분이라 코드 기준으로 다시 씀

**2. 커밋/배포 상태**

모든 변경사항 커밋 완료, `origin/main`에 푸시 완료 — 버그 수정 4건(`3c163df`→`27918ad`→`9ef815c`→`dbd8bdc`)에 이어 이번 CLAUDE.md 갱신 커밋까지 전부 반영됨(정확한 해시는 `git log` 최신 커밋 참고). Vercel 자동 배포 트리거됨 — **단, 이번 세션엔 배포 후 `thepmpo.com` 라이브 재확인은 하지 않았음** (아래 3번 참고). 이번 세션 작업분 외에 별도로 커밋 안 된 변경사항 없음(`.claude/launch.json`의 p6-clip-dev 설정 추가와 `docs/` 밑 다수의 untracked 문서는 이번 세션 이전부터 있던 별개 작업물이라 손대지 않고 그대로 둠).

**3. 다음 세션이 바로 이어서 할 작업**

**새로 요청받은 작업 없음.** 다음 세션은 새 작업을 시작하면 됨. 단, 아래 두 가지는 사용자 본인의 직접 확인이 아직 남아있어 재검토 요청이 들어올 수 있음.

- **댓글 레이아웃 변경(위 1번의 4번 항목, 커밋 `9ef815c`)은 Claude가 로그인 세션으로 직접 확인하지 못함** — `app/post/[id]/page.tsx`가 비로그인 시 전체를 `LoginPromptModal`로 막는 하드 게이트가 있어서(2026-08-04부터 의도된 정책, 아래 참고), 테스트 계정 없이는 실제 화면을 볼 수 없었음. `tsc`/`next build` 통과와 콘솔 에러 없음만 확인했고, 실제 스타일링/줄바꿈/메뉴 동작은 **사용자가 로그인해서 직접 눈으로 확인 필요**
- 이번 세션의 CLAUDE.md 대규모 갱신 내용(특히 2~4장)도 커밋 로그 기반으로 재구성한 것이라, 실제 화면과 다르게 서술된 부분이 있는지 다음 세션에서 필요시 한 번 더 대조하면 좋음

**4. 이번 세션에서 결정했지만 아직 확인 안 된 것들**

- **상대시간 포맷의 7일 이후 표기(`MM/DD`)는 사용자가 명시적으로 요청한 범위가 아님** — 사용자는 "24시간 이후 1일 전 등"까지만 언급, 7일 넘으면 어떻게 표기할지는 지정 안 해서 판단해서 추가함(`app/lib/relativeTime.ts`). 필요시 조정 가능
- **회원가입 `canSubmit` 조건에 `!nicknameError && !nicknameChecking` 추가** — 명시적 요청은 아니었으나, 중복 닉네임 에러가 뜬 상태에서 가입 버튼이 눌리는 걸 막기 위한 자연스러운 부수 결정이었음. 문제시 되돌릴 수 있음
- **프로필 수정 페이지의 닉네임 중복 체크는 기존 닉네임과 같으면 재확인을 건너뜀**(`checkNicknameDuplicate`에서 `trimmed === originalNickname`이면 return) — 자기 자신과 비교해서 불필요한 API 호출/에러를 막기 위한 판단이었음, 명시적 요청 아님

**5. 지난 세션에서 남겼던 미해결 메모의 정정**

- 직전 세션 핸드오프 메모에 "`app/post/[id]/page.tsx`의 비로그인 로그인 게이트가 IA 문서와 어긋나는 회귀 버그일 수 있다"고 적었던 부분은 **이번 세션에서 커밋 로그 확인 결과 오판이었음** — 회귀가 아니라 2026-08-04(`57653b5`) `Require login to view Circle post detail`에서 의도적으로 도입한 정책이며, 목록 클릭 단계와 상세 라우트 양쪽에서 우회 방지까지 설계된 확정 기능. 위 2장 "로그인 필요 여부" 절에 근거와 함께 정식 반영 완료 — 더 이상 "미해결 이슈"가 아님

**6. 다음 세션 시작 시 참고**

- **최신 커밋**: `main` 브랜치, `origin` 동기화 완료(이번 세션의 버그수정 커밋 `9ef815c`/`dbd8bdc`에 이어 이번 CLAUDE.md 갱신 커밋까지 최신)
- **로컬 개발 서버**: `npm run dev`, `localhost:3000`. iCloud Drive 안 프로젝트라 `.next` 폴더에 동기화 충돌 파일이 가끔 생김 — 이상한 타입 에러 나면 `.next` 폴더째 지우고 재시작하면 해결(빌드 산출물이라 삭제 안전)
- **백로그(낮은 우선순위, 필요 시 착수)**: "답변완료" 배지 UI, 로고 코랄 포인트 요소, Circle 검색 기능, Circle/게시글 공유, 대댓글, 카테고리 분류, 북마크, 영상 업로드, 프로필 사진(목록·상세 노출), `/notice` 실제 콘텐츠(현재 내비 링크 없는 placeholder) — 상세는 위 "백로그" 절 참고

### 개발 액션 아님 (운영자가 직접 처리)

- 법적 문서(`/terms`, `/privacy`) 최종 문구 변호사 검토 — 개발/기입은 완료, 검토만 남음
- Insights에 실제 콘텐츠 발행 — 기능은 완성되어 있으나 글이 0건이라 실제로 볼 콘텐츠가 없음

---

## 6. 작업 원칙 (Claude Code가 참고할 것)

- 사용자는 개발 지식이 없음 — 기술적 설명보다 "왜 이렇게 구현하는지"를 제품 관점에서 설명
- 이 프로젝트는 창업 + 포트폴리오 목적 — 프로덕션 수준의 보안/데이터 처리(RLS, 회원탈퇴 정책 등)를 가볍게 넘기지 말 것
- 새로운 기능/정책을 결정하게 되면 이 CLAUDE.md도 함께 업데이트
- PRD에 없는 기능을 임의로 추가하지 말 것 — 필요하면 먼저 논의 후 PRD에 반영
